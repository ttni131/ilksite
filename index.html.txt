<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ttni131 Sunucu Tanıtımı</title>
    <style>
        /* MINECRAFT TEMALI TASARIM */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', sans-serif;
        }

        body {
            /* Minecraft dünyasını anımsatan bir arka plan */
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('https://images.unsplash.com/photo-1587573089734-09cb69c0f2b4?q=80&w=1974&auto=format&fit=crop');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
        }

        .server-card {
            background: rgba(0, 0, 0, 0.8);
            border: 3px solid #555;
            padding: 40px;
            border-radius: 10px;
            text-align: center;
            max-width: 600px;
            width: 90%;
            box-shadow: 0 0 20px rgba(0,0,0,0.5);
        }

        h1 {
            color: #55FF55; /* Minecraft yeşili */
            font-size: 2.5rem;
            margin-bottom: 10px;
            text-transform: uppercase;
        }

        .status {
            display: inline-block;
            padding: 5px 15px;
            background: #55FF55;
            color: black;
            border-radius: 20px;
            font-weight: bold;
            margin-bottom: 20px;
            font-size: 0.9rem;
        }

        p {
            font-size: 1.1rem;
            margin-bottom: 30px;
            color: #ddd;
        }

        .ip-box {
            background: #222;
            padding: 15px;
            border: 2px dashed #555;
            font-family: monospace;
            font-size: 1.2rem;
            margin-bottom: 25px;
            cursor: pointer;
            transition: 0.3s;
        }

        .ip-box:hover {
            border-color: #55FF55;
            background: #2a2a2a;
        }

        .btn-join {
            display: inline-block;
            background: #55FF55;
            color: black;
            padding: 15px 30px;
            text-decoration: none;
            font-weight: bold;
            font-size: 1.1rem;
            border-bottom: 4px solid #389e38;
            transition: 0.2s;
        }

        .btn-join:active {
            transform: translateY(4px);
            border-bottom: none;
        }
    </style>
</head>
<body>

    <div class="server-card">
        <div class="status">SUNUCU AKTİF</div>
        <h1>ttni131 Sunucusu</h1>
        <p>Aternos üzerinden kurduğumuz bu muhteşem dünyaya hepiniz davetlisiniz! Hayatta kalma mücadelesine hemen katıl.</p>
        
        <div class="ip-box" onclick="alert('IP Adresi Kopyalandı!')">
            IP: ttni131.aternos.me
        </div>

        <a href="https://aternos.org" target="_blank" class="btn-join">ŞİMDİ KATIL</a>
    </div>

</body>
</html>